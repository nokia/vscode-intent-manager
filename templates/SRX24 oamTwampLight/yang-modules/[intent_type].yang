module {{ intent_type }} {
  namespace "http://www.nokia.com/management-solutions/{{ intent_type }}";
  prefix {{ intent_type }};
  
  organization
    "{{ author }}";
  contact
    "{{ author }}";
  description
    "";

  revision "{{ date }}" {
    description
      "Initial revision.";
  }

  container {{ intent_type }} {
    leaf endpoint-a {
        type string;
        mandatory true;
    }
    leaf endpoint-b {
        type string;
        mandatory true;
    }
  }
}
