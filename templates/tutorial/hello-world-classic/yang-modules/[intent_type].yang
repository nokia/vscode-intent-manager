module {{ intent_type }} {
  namespace "urn:nokia.com:nsp:yang:im:{{ intent_type }}";
  prefix "{{ intent_type }}";

  organization "{{ author }}";
  contact "";
  description "";

  revision "{{ date }}" {
    description
      "Initial revision.";
  }

  container {{ intent_type }} {
  }
}